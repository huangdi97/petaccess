# COPY_GUIDE.md

> 中性文案规范 · 对应 `UI_UX_IMPLEMENTATION_SPEC.md` §8 与 `WORKBUDDY_PRODUCTION_MASTER_GOAL.md` P2 §4.6
> 机读约束：`packages/design-tokens/src/index.ts` 的 `REQUIRED_COPY` / `FORBIDDEN_COPY`
> 强制手段：`tests/unit/test_design_tokens.py::test_no_forbidden_copy_in_user_facing_sources`（扫描全部前台源码）

---

## 1. 为什么文案是产品的一部分

本产品回答的是「**边界**」，不是「**好坏**」。任何把规则差异表达为价值判断的措辞，都会把一个中性的准入事实变成对场所或人群的评价，从而：
- 误导用户（"明确限制"不等于"这家店不好"）；
- 把平台置于争议中（评价商户、评价养宠人群）；
- 违反 ADR-014（用户自选过滤是中性，编辑性道德排序不是）。

---

## 2. 状态文案（强制）

| 场景 | 必须使用 | 禁止使用 |
|---|---|---|
| 规则明确许可 | **明确允许** | 允许进入 / 可进 / 友好 |
| 附条件许可 | **有条件** | 基本可以 / 需注意 |
| 规则明确禁止 | **明确限制** | 禁止 / 雷店 / 不能去 |
| 无已核验规则 | **尚未核验** | 未知 / 待定 / 允许（**绝不可显示为允许**） |
| 来源结论冲突 | **来源存在不一致** | 有争议 / 有问题 |
| 证据超时效 | **需要复核** | 过期 / 失效 |

完整语义表见 `DESIGN_SYSTEM.md` §3。

---

## 3. 完整句式模板

| 用途 | 文案 |
|---|---|
| 空状态（长） | 截至今日，在已核验来源中暂未找到明确规则 |
| 空状态（短） | 尚未核验 |
| 现场记录免责 | 现场记录 ≠ 场所正式政策 |
| 无排名声明 | 本产品不提供综合评分或场所排名 |
| 补充输入 | 需要补充：{字段}（不猜测） |
| 归属未定 | 地址未收录 |
| 来源未定 | 待收录 |
| 权限失败 | 需要登录后才能{操作} |
| 规则变化上报成功 | 已记录：规则已变化，进入复核 |

---

## 4. 禁用词表（机读，禁止出现在任何前台源码）

`雷店` · `雷区` · `黑榜` · `红榜` · `恶心` · `脏` · `没素质` · `反宠` · `爱宠人士` · `文明指数` · `遇宠率` · `星级`

> 该表由测试强制。新增禁用词只需加入 `FORBIDDEN_COPY`，测试自动覆盖全部 `.vue` / `.ts` 前台文件（注释除外，因此可以在文档中讨论这些词）。

---

## 5. 术语一致性

| 概念 | 统一表述 | 不采用 |
|---|---|---|
| 生物学范围犬只 | 犬只 | 狗狗 / 汪星人 |
| 宠物一般性条款主体 | 普通宠物 | 宠物 |
| 工作犬角色 | 服务犬 | 工作犬（前台） |
| 场所 | 场所 | 门店 / 店铺（混用） |
| 分区 | 区域 | 区块 |
| 规则来源 | 来源 | 出处（混用） |
| 核验时间 | 最近核验 | 更新时间（混用） |

---

## 6. 语气

- 陈述事实，不劝告、不评判：写「该区域明确限制」，不写「请注意不要带宠物进去哦」。
- 不使用感叹号与 emoji（状态 icon 是语义通道，不是情绪装饰）。
- 不确定就说不确定：`UNKNOWN` 必须显式呈现为「尚未核验」，不得用留白暗示允许。
- 不用「我们建议」类主观引导；用「来源显示」「管理方规则」类可追溯表述。

---

## 7. 违规处理

前台文案若出现禁用词，`tests/unit/test_design_tokens.py` 会在 CI 失败并逐条列出文件与词。若确需在文档、注释或后台调试界面使用，请置于注释内（守卫会剥离注释）或置于 `apps/admin` 之外的非前台目录。
